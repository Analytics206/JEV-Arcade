# Secrets: your keys live here and never in git
.env

# Your own deployment beside compose.yaml (a tunnel, a proxy), and its own
# files at the site's root (WIKIRACE_SITE_ROOT: a search console's file, say)
compose.*.yaml
compose.*.yml
site-root/

# Race history
data/
*.db
*.db-journal
*.db-wal
*.db-shm

# Python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
build/
dist/
*.egg-info/

# Editors and OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Node, used only to test the page's pure state
node_modules/
